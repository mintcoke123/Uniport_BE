# 프로덕션 프로파일. 배포 시 SPRING_PROFILES_ACTIVE=prod 필수.
# PaaS 포트: server.port=${PORT:8080}. DB/CORS는 env 필수.

server:
  port: ${PORT:8080}
  forward-headers-strategy: framework

spring:
  h2:
    console:
      enabled: false
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
    open-in-view: false

# 프로덕션 로깅: SQL/바인더 로그 비활성화
logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN

# CORS: 배포된 프론트(Vercel) 도메인. allowCredentials=true 시 정확한 도메인만 허용. 여러 개는 쉼표로.
app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}
  friend-invite:
    base-url: ${APP_FRIEND_INVITE_BASE_URL:https://uniportbe-production.up.railway.app}
    expiration-days: ${APP_FRIEND_INVITE_EXPIRATION_DAYS:7}

# JWT: 프로덕션에서는 env JWT_SECRET 필수(32자 이상 랜덤). 기본값 사용 금지.
jwt:
  secret: ${JWT_SECRET:}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

# 시드/어드민: 프로덕션에서 테스트 유저 시드 비활성화, 어드민·준관리자 비밀번호 강한 값으로 설정
uniport:
  admin:
    student-id: ${UNIPORT_ADMIN_STUDENT_ID:22011739}
    password: ${UNIPORT_ADMIN_PASSWORD:uniport}
  sisu-admin:
    student-id: ${UNIPORT_SISU_ADMIN_STUDENT_ID:26999999}
    password: ${UNIPORT_SISU_ADMIN_PASSWORD:SISUadmin123!!}
  seed:
    test-user-enabled: ${UNIPORT_SEED_TEST_USER_ENABLED:false}
  education:
    seed:
      force-refresh: ${UNIPORT_EDUCATION_SEED_FORCE_REFRESH:true}

asset:
  master:
    us:
      import:
        enabled: ${ASSET_MASTER_US_IMPORT_ENABLED:false}
        on-startup:
          enabled: ${ASSET_MASTER_US_IMPORT_ON_STARTUP_ENABLED:true}
